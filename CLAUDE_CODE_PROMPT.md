## البروبمت (انسخ من هنا)

ابنيلي موقع ويب لدعوة فرح، صفحة واحدة (single page)، بالمواصفات دي بالظبط:

### الستاك التقني

- Vite + React + TypeScript
- Tailwind CSS v4 (استخدم `@tailwindcss/vite` plugin، مش الطريقة القديمة بـ tailwind.config.js)
- Framer Motion لكل الحركات والانتقالات
- بدون أي backend أو قاعدة بيانات — كل حاجة client-side

### الستايل والتصميم

- الفايب: رومانسي وناعم (romantic & soft)، مش مودرن جامد ومش زهور فاقعة.
- الألوان: خلفية عاجي/كريمي فاتح (ivory)، تفاصيل بلون وردي باهت (blush rose)، لمسات ذهبي عتيق (antique gold) للخطوط والزخارف، ونص بلون خمري غامق (deep wine) للعناوين.
- الخطوط:
  - العربي: "Aref Ruqaa" للعناوين والأسماء (خط عربي كلاسيكي/خطاطي)، و"Tajawal" للنصوص العادية.
  - الإنجليزي: "Playfair Display" للعناوين، و"Cormorant Garamond" للنصوص العادية.
  - حمّل الخطوط دي من Google Fonts.
- زخارف: فروع/ورود بسيطة SVG بألوان ذهبي/وردي في زوايا الأقسام (مش صور، ارسمها SVG عشان الموقع يفضل خفيف).
- حركات دخول ناعمة (fade + slide up) لكل قسم لما يظهر على الشاشة (Framer Motion + whileInView).

### اللغة (مهم جدًا)

- الموقع لازم يدعم لغتين: عربي (افتراضي، RTL) وإنجليزي (LTR).
- زرار تبديل لغة ثابت في الزاوية يبدّل كل نصوص الموقع + اتجاه الصفحة (`dir`) + الخط المستخدم.
- خزّن كل النصوص (عربي/إنجليزي) في ملف واحد منفصل عن باقي الكود (مثلاً `src/data/content.ts`) عشان يبقى سهل التعديل بعدين.

### محتوى الدعوة (استخدمه حرفيًا)

**العريس:** محمد (بن سعد مختار سعد)
**العروسة:** غادة (بنت محمد أنور)

**آية الافتتاح:** بِسْمِ اللَّهِ الرَّحْمَـٰنِ الرَّحِيمِ

**أبيات الدعوة:**

> دَامَتْ قطُوفُ المنَى، وَالسَعْدُ مُبْتَسِمُ — وَطَابَ فِي لَيْلَةِ الأَفْرَاحِ جَمْعُكُمُ
> فَأَقْبِلُوا كَيْ يَتِمَّ النُّورُ مُكْتَمِلًا — فَإِنَّمَا عِطْرُ هَذَا الحَفْلِ وَصْلُكُمُ

**نص الترحيب:** بِقُلُوبٍ تَفِيضُ بِالمَحَبَّةِ، وَتَتَّسِعُ لِفَرْحَةِ اللِّقَاءِ، يَتَشَرَّفُ كُلٌّ مِن:
الشيخ/ سعد مختار سعد و الرائد/ محمد أنور
بدعوتكم لحضور حفل زفاف نجليهما

**التاريخ:** مساء الخميس، ١٠ سبتمبر ٢٠٢٦
**الموعد:** بعد صلاة العشاء (استخدم تقريبًا الساعة ٨:٣٠ مساءً بتوقيت القاهرة UTC+3 لحساب العداد التنازلي، مع تعليق واضح في الكود يوضح إنه ممكن يتغير)
**المكان:** قاعة الأكابر، القناطر الخيرية، مصر

للترجمة الإنجليزية استخدم صياغة حرفية للشعر العربي)، مثلاً:

### الأقسام المطلوبة (بالترتيب)

1. **Hero**: البسملة، الأبيات، نص الترحيب، أسماء العروسين بخط كبير وواضح (محمد و غادة)، مع زخارف SVG في الزوايا.
2. **Countdown**: عداد تنازلي حي (أيام / ساعات / دقائق / ثواني) لحد ميعاد الفرح، بتصميم كروت أنيقة وانيميشن لما الرقم يتغير.
3. **Details**: كروت فيها التاريخ، الموعد، والمكان.
4. **RSVP**: فورم بسيط (الاسم، هل هيحضر ولا لأ)،
5. **Footer**: جملة شكر بسيطة زي "بحضوركم تكتمل فرحتنا 🤍" مع زخرفة SVG.

### حاجات تقنية مهمة

- استخدم React functional components + hooks بس، من غير أي مكتبة state management خارجية.
- اعمل Context (`LanguageContext`) لإدارة اللغة الحالية والمحتوى المرتبط بيها، ويتغيّر `document.documentElement.lang` و `dir` تلقائيًا لما اللغة تتغير.
- الكود لازم يكون منظم في components منفصلة (Hero, Countdown, Details, RSVP, Footer, LanguageToggle, FloralOrnament) جوه `src/components/`.
- تأكد إن `npm run build` بيشتغل من غير أي أخطاء TypeScript قبل ما تسيبني.

---
